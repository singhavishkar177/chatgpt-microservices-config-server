spring:
  application:
    name: user-service1
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chatgpt_microservice
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
